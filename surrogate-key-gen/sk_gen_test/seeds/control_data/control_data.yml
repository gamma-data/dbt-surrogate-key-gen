---
version: 2

seeds:
  - name: x_key_map
    config:
      enabled: false
      column_types:
        source: string
        next_sk: integer
  - name: x_km_sample_data
    config:
      enabled: false
      column_types:
        id: integer
        src_key: string